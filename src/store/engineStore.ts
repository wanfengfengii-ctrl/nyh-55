import { create } from 'zustand'; import type {   EngineState,   EngineConfig,   ComputationStep,   AnimationDetail,   StateSyncPayload,   ControlPayload, } from '@/types'; import { DEFAULT_CONFIG } from '@/types'; import { createEngineState, executeStep, deepCloneState } from '@/engine/DifferenceEngine'; import { useCollabStore } from './collabStore'; import { useRecordingStore } from './recordingStore'; import { consistencyChecker } from '@/collaboration/ConsistencyChecker';  type DemoControlAction = 'step_forward' | 'step_back' | 'reset' | 'continuous_start' | 'continuous_stop' | 'initialize';  interface EngineStore {   engineState: EngineState | null;   config: EngineConfig;   isAnimating: boolean;   animationDetail: AnimationDetail | null;   operationLog: ComputationStep[];   historyStack: EngineState[];   isInitialized: boolean;   isRunning: boolean;   displayPhase: EngineState['phase'];   syncMode: 'local' | 'host' | 'audience';   lastSyncSequence: number;   consistencyCheckInterval: ReturnType<typeof setInterval> | null;    initialize: (config?: Partial<EngineConfig>) => void;   stepForward: () => void;   stepBack: () => void;   reset: () => void;   setAnimating: (v: boolean) => void;   setAnimationDetail: (d: AnimationDetail | null) => void;   updateConfig: (partial: Partial<EngineConfig>) => void;   startContinuous: () => void;   stopContinuous: () => void;   continuousTick: () => void;   setDisplayPhase: (p: EngineState['phase']) => void;   setIsRunning: (v: boolean) => void;    collabStepForward: () => void;   collabStepBack: () => void;   collabReset: () => void;   collabStartContinuous: () => void;   collabStopContinuous: () => void;   collabInitialize: (config?: Partial<EngineConfig>) => void;   applyStateSync: (payload: StateSyncPayload) => void;   handleRemoteControl: (type: string, payload?: ControlPayload) => void;   enableConsistencyChecks: () => void;   disableConsistencyChecks: () => void;   broadcastState: () => void;   setSyncMode: (mode: EngineStore['syncMode']) => void;   recordAction: (action: DemoControlAction) => void; }  function cloneState<T>(obj: T): T {   return JSON.parse(JSON.stringify(obj)); }  export const useEngineStore = create<EngineStore>((set, get) => ({   engineState: null,   config: { ...DEFAULT_CONFIG },   isAnimating: false,   animationDetail: null,   operationLog: [],   historyStack: [],   isInitialized: false,   isRunning: false,   displayPhase: 'idle',   syncMode: 'local',   lastSyncSequence: 0,   consistencyCheckInterval: null,    initialize: (configOverride) => {     const cfg = { ...get().config, ...configOverride };     try {       const state = createEngineState(cfg);       set({         engineState: state,         config: cfg,         isInitialized: true,         operationLog: [],         historyStack: [],         isAnimating: false,         animationDetail: null,         isRunning: false,         displayPhase: 'idle',       });     } catch (e) {       console.error('鍒濆鍖栧樊鍒嗘満澶辫触:', e);     }   },    stepForward: () => {     const { engineState, isAnimating, isRunning } = get();     if (!engineState || isAnimating) return;     if (engineState.phase === 'error' || engineState.phase === 'complete') return;      const snapshot = deepCloneState(engineState);     const result = executeStep(engineState, engineState.currentStep);     const hasCarry = result.animation.carryTriggers.length > 0;      set((s) => ({       engineState: result.newState,       operationLog: [...s.operationLog, result.log],       historyStack: [...s.historyStack, snapshot],       animationDetail: result.animation,       isAnimating: true,       displayPhase: 'adding',     }));      if (hasCarry) {       setTimeout(() => {         if (get().isAnimating) set({ displayPhase: 'carrying' });       }, 500);     }     if (result.error && isRunning) set({ isRunning: false });   },    stepBack: () => {     const { historyStack, isAnimating, isRunning } = get();     if (isAnimating || isRunning || historyStack.length === 0) return;     const prevState = historyStack[historyStack.length - 1];     set((s) => ({       engineState: prevState,       historyStack: s.historyStack.slice(0, -1),       operationLog: s.operationLog.slice(0, -1),       animationDetail: null,       isAnimating: false,       displayPhase: prevState.phase,     }));   },    reset: () => {     set({       engineState: null,       isInitialized: false,       operationLog: [],       historyStack: [],       isAnimating: false,       animationDetail: null,       isRunning: false,       displayPhase: 'idle',     });   },    setAnimating: (v) => {     set({ isAnimating: v });     if (!v) {       const { isRunning, engineState } = get();       set({ displayPhase: engineState?.phase ?? 'idle' });       if (isRunning && engineState) {         if (engineState.phase === 'error' || engineState.phase === 'complete') {           set({ isRunning: false });         }       }     }   },   setAnimationDetail: (d) => set({ animationDetail: d }),   setDisplayPhase: (p) => set({ displayPhase: p }),   setIsRunning: (v) => set({ isRunning: v }),   updateConfig: (partial) => set((s) => ({ config: { ...s.config, ...partial } })),    startContinuous: () => {     const { engineState, isAnimating } = get();     if (!engineState || isAnimating) return;     if (engineState.phase === 'error' || engineState.phase === 'complete') return;     set({ isRunning: true });   },   stopContinuous: () => set({ isRunning: false }),    continuousTick: () => {     const { isRunning, isAnimating, engineState } = get();     if (!isRunning || isAnimating) return;     if (!engineState || engineState.phase === 'error' || engineState.phase === 'complete') {       set({ isRunning: false });       return;     }     get().stepForward();   },    collabInitialize: (configOverride) => {     const collab = useCollabStore.getState();     if (collab.isInSession && !collab.canControl()) return;     get().initialize(configOverride);     if (collab.isInSession && collab.isPresenter()) {       collab.sendInitialize(configOverride || {});       get().broadcastState();       get().recordAction('initialize');     }   },    collabStepForward: () => {     const collab = useCollabStore.getState();     if (collab.isInSession && !collab.canControl()) return;     const { engineState, isAnimating } = get();     if (!engineState || isAnimating) return;     if (engineState.phase === 'error' || engineState.phase === 'complete') return;      get().stepForward();     if (collab.isInSession && collab.isPresenter()) {       collab.sendControlCommand('control_step_forward');       setTimeout(() => get().broadcastState(), 1300);       get().recordAction('step_forward');     }   },    collabStepBack: () => {     const collab = useCollabStore.getState();     if (collab.isInSession && !collab.canControl()) return;     get().stepBack();     if (collab.isInSession && collab.isPresenter()) {       collab.sendControlCommand('control_step_back');       get().broadcastState();       get().recordAction('step_back');     }   },    collabReset: () => {     const collab = useCollabStore.getState();     if (collab.isInSession && !collab.canControl()) return;     get().reset();     if (collab.isInSession && collab.isPresenter()) {       collab.sendControlCommand('control_reset');       get().broadcastState();       get().recordAction('reset');     }   },    collabStartContinuous: () => {     const collab = useCollabStore.getState();     if (collab.isInSession && !collab.canControl()) return;     get().startContinuous();     if (collab.isInSession && collab.isPresenter()) {       collab.sendControlCommand('control_continuous_start');       get().recordAction('continuous_start');     }   },    collabStopContinuous: () => {     const collab = useCollabStore.getState();     if (collab.isInSession && !collab.canControl()) return;     get().stopContinuous();     if (collab.isInSession && collab.isPresenter()) {       collab.sendControlCommand('control_continuous_stop');       get().recordAction('continuous_stop');     }   },    applyStateSync: (payload) => {     if (payload.sequence <= get().lastSyncSequence) return;     set({       engineState: payload.engineSnapshot ? cloneState(payload.engineSnapshot) : null,       operationLog: payload.operationLog ? cloneState(payload.operationLog) : [],       historyStack: payload.historyStack ? cloneState(payload.historyStack) : [],       isAnimating: payload.isAnimating,       isRunning: payload.isRunning,       displayPhase: payload.displayPhase,       config: payload.config ? { ...payload.config } : get().config,       isInitialized: payload.engineSnapshot !== null,       lastSyncSequence: payload.sequence,     });     useCollabStore.getState().setSyncStatus('synced');   },    handleRemoteControl: (type, payload) => {     useCollabStore.getState().setSyncStatus('syncing');     switch (type) {       case 'control_step_forward':         get().stepForward();         break;       case 'control_step_back':         get().stepBack();         break;       case 'control_reset':         get().reset();         break;       case 'control_continuous_start':         get().startContinuous();         break;       case 'control_continuous_stop':         get().stopContinuous();         break;       case 'control_initialize':         get().initialize(payload?.config);         break;     }   },    broadcastState: () => {     const collab = useCollabStore.getState();     if (!collab.isInSession || !collab.isPresenter()) return;     const s = get();     collab.sendStateSync({       engineSnapshot: s.engineState ? cloneState(s.engineState) : null,       operationLog: cloneState(s.operationLog),       historyStack: cloneState(s.historyStack),       isAnimating: s.isAnimating,       isRunning: s.isRunning,       displayPhase: s.displayPhase,       config: { ...s.config },     });   },    recordAction: (action: DemoControlAction) => {     const rec = useRecordingStore.getState();     if (rec.isRecording) {       setTimeout(() => rec.recordStep(action), 50);     }   },    enableConsistencyChecks: () => {     get().disableConsistencyChecks();     const interval = setInterval(async () => {       const collab = useCollabStore.getState();       if (!collab.isInSession) return;       const engine = useEngineStore.getState();       if (!engine.isInitialized) return;       const report = await consistencyChecker.checkCurrentState();       if (!report.matches) {         collab.setSyncStatus('out_of_sync');       }     }, 5000);     set({ consistencyCheckInterval: interval });   },    disableConsistencyChecks: () => {     const interval = get().consistencyCheckInterval;     if (interval) {       clearInterval(interval);       set({ consistencyCheckInterval: null });     }   },    setSyncMode: (mode) => set({ syncMode: mode }), }));